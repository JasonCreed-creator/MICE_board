# CLAUDE.md — 팀 프로젝트 보드 (Team Board)

> 불변 컨텍스트. 세션(Cowork·Code)이 이 폴더를 열 때 가장 먼저 읽는다. 가변 상태는 `PROGRESS.md`.
> 규약: 폴더 = 세션 · 한 폴더 동시 1세션 · 시작 시 체크인, 종료 시 체크아웃(PROGRESS.md 갱신).

## 1. 프로젝트 정의
- 목적: 마이스 비즈 팀(리멤버앤컴퍼니)의 프로젝트를 **구글 스프레드시트에 기록**하고, **Apps Script 웹앱 대시보드** 한 판에서 담당자 배정 · M/D 가동률 · Input(M/D) vs Output(매출·마진·행사 성과)을 관리한다.
- 한 줄 경계: "누가 어디에 얼마나 들어가 있고, 그 투입이 무엇을 만들어내는가"만 답한다. 마일스톤 단위 **업무 블럭·계획 공수·주석**까지가 보드 범위(D11). 일정 의존관계·산출물·태스크 단위 관리는 커뮤니케이터 영역.
- 설계 정본: `docs/SPEC-v1.md` (데이터 모델·지표 산식·화면·기술 구조·착수 조건). 실무 계약: `docs/DATA-CONTRACT.md`. 편집 규칙·롤업 코드: `src/schema.js`. 시트 탭 11개: 프로젝트·배정·공수기록·팀원·마일스톤·정산·설정·변경이력·세부항목·주석·업무블럭.

## 2. 확정 결정 (변경 시 SPEC 개정 동반)
| # | 결정 | 일자 |
|---|---|---|
| D1 | 대시보드 = Apps Script 웹앱(시트 바인딩), 단일 index.html + Code.gs | 2026-09-10 |
| D2 | 작업 범위 [B] 3턴: 1 기획 → 2 빌드 → 3 검수 (이후 턴은 기획자 지시로 추가) | 2026-09-10 |
| D3 | Input = M/D 주간 기록만. 단가·인건비 금액 환산 **금지** | 2026-09-10 |
| D4 | Output = 매출 · 마진(3층 25·10·35) · 행사 성과(참가·쇼업률). 파이프라인 전환율 제외 | 2026-09-10 |
| D5 | 산출물 위치 = 이 폴더. Code 인계 전제 | 2026-09-10 |
| D6 | **대시보드 편집(4턴)**: 프로젝트·팀원·공수기록·마일스톤·정산을 대시보드에서 추가·수정·삭제. 배정은 기존 편집기. 설정 탭은 시트 전용 | 2026-09-13 (기획자 지시 · 세부 확인 대기) |
| D7 | `변경이력` 탭 신설 — 대시보드 쓰기 전부 기록(일시·사용자·탭·키·동작·변경 내용·이전 행 백업) | 2026-09-13 |
| D8 | 삭제 규칙 — 공수기록·정산 매출이 있는 프로젝트는 삭제 불가(`드롭`) · 참조되는 팀원은 삭제 불가(`퇴사`) · 프로젝트 삭제는 배정·마일스톤·정산 행 연쇄(백업 후) | 2026-09-13 |
| D9 | 동시 편집 = 서버 잠금 + 편집 시작 시점 스냅샷(`expected`) 대조, 불일치 시 저장 거부 | 2026-09-13 |
| D10 | 팀원 이름 변경은 대시보드에서 불가(5탭 참조키) | 2026-09-13 |
| D11 | **5턴 경계 조정**: 마일스톤 아래 파트별 업무 블럭·계획 공수·주석까지 보드 범위. 일정 의존관계·산출물 관리는 커뮤니케이터 | 2026-09-14 |
| D12 | 단위: 저장·산식 M/D, 화면 합계에 M/M 환산(÷ 월 가용 M/D 20) 병기 | 2026-09-14 |
| D13 | 세부 항목 (담당·파트)별 합계 → `배정` 행 자동 반영(비고 `자동(세부항목)`). 같은 (담당·파트)에 수동 행이 있으면 자동 행 생략 + 겹침 표시 | 2026-09-14 |
| D14 | 파트 = 역할 6종. 파트별 업무 블럭 카탈로그(`업무블럭` 탭, 없으면 코드 기본값)에서 골라 넣고 블럭마다 M/D | 2026-09-14 |
| D15 | 팀원 등급 없음. 임팩트·난이도는 표시만, 배정 담당자가 판단 | 2026-09-14 |
| D16 | 주석: 유형 4종 · 작성자 이메일 자동 + 이름 선택 · 블럭의 "판단에 필요한 내용"을 첫 주석으로 · 본인만 수정 · 삭제는 시트 | 2026-09-14 |
| D17 | `팀원` 탭 **`이메일`(F열)** 추가 — 선택 입력, 로그인 계정 ↔ 팀원 자동 매칭용. 회사 업무 계정만(개인 휴대폰·개인 메일은 계속 금지) | 2026-09-15 |
| D18 | **내 주간 공수** 한 화면 — 내 배정 프로젝트 자동 제시 · 지난주 값 복사 · 합계·초과 경고 · 한 번에 저장 | 2026-09-15 |
| D19 | **변경이력 되돌리기** — 백업이 있는 이력 행을 1클릭 복원, 복원도 `되돌림` 으로 기록. 프로젝트 삭제(연쇄)는 미지원 | 2026-09-15 |
| D20 | **배정 저장 가드** — 저장으로 사라지는 행이 있으면 인라인 경고 + 2단계 확인 | 2026-09-15 |
| D21 | **마일스톤 일괄 처리** — 다중 선택 → 완료 처리 / 예정일 조정. 변경이력 묶음 1건(백업 배열) | 2026-09-15 |
| D22 | **업무 블럭 카탈로그 파생** — 역할 목록에 있는데 블럭이 0개인 파트는 공통 블럭 5종을 파생. 운영총괄·운영 Sub 전용 블럭 기본 카탈로그에 추가(33 → 44) | 2026-09-15 |
| D23 | **CI 게이트** — GitHub Actions 가 push·PR 마다 빌드 재현성·테스트·실렌더·정적 검사 실행 | 2026-09-15 |
| D24 | **clasp 배포** — 로컬 `npm run deploy` + `main` 병합 시 Actions 자동 배포. 배포 ID 고정으로 웹앱 주소 유지. 자격증명은 GitHub Secret | 2026-09-15 |

## 3. 작업 규칙
- **시트가 SoT**: 대시보드는 시트를 직접 읽고 쓴다(별도 동기화 없음). 쓰기는 `docs/SPEC-v1.md` §5.2 에 열거된 경로만 — 새 경로를 만들면 SPEC·계약·`src/schema.js`·Code.gs·mock·테스트·게이트를 함께 고친다.
- **모든 쓰기 = 잠금 → 검증 → 충돌 검사 → 행 단위 부분 쓰기(수식 열 보존) → 변경이력 기록**. 삭제·연쇄 작업은 2단계 인라인 확인. 브라우저 대화상자(alert·confirm) 금지.
- **두 모드 한 파일**: `index.html`은 mock 모드(인라인 샘플 JSON, 더블클릭 확인)와 gas 모드(`google.script.run`)를 `DataProvider` 어댑터로 분리. 산식(`src/metrics.js`)·편집 규칙(`src/schema.js`)은 순수 함수로 두어 Node 테스트로 검산.
- **`src/schema.js` 는 세 곳에서 같은 파일이 실행된다**: 브라우저(빌드 인라인) · Node 테스트 · Apps Script(`scripts/build.js` 가 `Code.gs` 의 `__SCHEMA_BEGIN__/__SCHEMA_END__` 블록에 채움). Code.gs 의 그 블록은 손으로 고치지 않는다.
- **외부 라이브러리 금지**: 차트는 SVG 직접 렌더(오프라인·CSP 무관).
- **라벨 규칙**: 한국어 기본, 업계 약어(PCO·A/V·sqm 등) 금지 — 팀원 누구나 읽히게.
- **민감 정보**: 단가·급여·개인 연락처는 어느 탭에도 두지 않는다.
- **룩**: 리멤버 웜 페이퍼(jc-remember-html). 기존 토큰 사용, 신규 색 최소화.
- **비개발자 기준 가이드**: 설치·배포·운영은 "어느 메뉴에서 무엇을 클릭"까지 적는다.
- **검수 게이트**: 빌드 → 테스트(`node --test`) → 실렌더(`scripts/render-check.js`, 탭 전수 + 쓰기 경로 전수) → 정적 검사 → jc-redteam. 정적 캡처만으로 통과 처리 금지.

## 4. 폴더 구조
```
CLAUDE.md / PROGRESS.md / package.json / .clasp.json.example (실제 .clasp.json 은 커밋하지 않는다)
.github/       workflows/gate.yml(검수 게이트 자동 실행) · workflows/deploy.yml(clasp 배포)
docs/          SPEC-v1.md · DATA-CONTRACT.md · code-brief-T*.md
src/           index.template.html · styles.css · schema.js · metrics.js · app.js   (원본 — 여기만 고친다)
scripts/       build.js(단일 파일 빌드 + Code.gs 블록) · render-check.js(Chrome 헤드리스 실렌더 게이트) · static-check.js(정적 검사)
tests/         metrics.test.js · sample-data.test.js · schema.test.js
apps-script/   Code.gs(손 유지 + 빌드 블록) · index.html(빌드 산출물) · appsscript.json
mock/          sample-data.json
preview/       dashboard-preview.html(빌드 산출물, mock 모드) · screenshots/
guide/         설치-운영-가이드.md
data/          실제 등록용 TSV 등 입력 자료
```

## 5. Code 세션 권장 설정
- 모델: **Fable 계열(현재 5.1)** · Effort **표준** — Apps Script 연동·배포 가이드 파트는 **엑스트라**
- 서브에이전트 분할(병렬, 파일 단위로 겹치지 않게): ① 서버 `Code.gs` ② 화면 `src/app.js`·`styles.css`·`render-check.js` ③ 문서·테스트 → 메인이 계약(`docs/DATA-CONTRACT.md`·`src/schema.js`) 기준으로 병합
- 저장소 전체 git 명령(stash·reset·checkout)은 병렬 에이전트에서 금지
- 환경은 두 가지다 — **클라우드 세션(GitHub)**: `node`·`npm`·Chromium 모두 있음, 게이트 전부 실행 가능, 단 Apps Script 직접 반영·시트 쓰기는 불가(읽기만). **로컬 PC**: `node` 가 PATH 에 없고 npm 도 없음(`PROGRESS.md` 환경 메모의 경로 사용), Bash 히어독은 10KB 초과·백슬래시에 취약 → 큰 파일은 Write 도구. 어느 쪽인지 먼저 확인하고 시작한다

## 6. 관련 자산 (참조만, 복제 금지)
- Slack 워크플로 "성사 프로젝트 등록"·"MICE 계약완료" 폼 필드 ↔ `프로젝트` 탭 1:1 호환
- 플레이북 08 역할(내부 6역할) · 00 허브 4유형 · 마일스톤 D-오프셋
- MICE 커뮤니케이터 정산보드(마진 3분할) — 정합은 결정 대기 #24
- 기획자님 시트·Apps Script 주소, Chrome 확장 운용법은 `PROGRESS.md` 와 메모리 `chrome-ext-sheets-appsscript-ops`
