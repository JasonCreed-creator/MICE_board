# PROGRESS.md — 팀 프로젝트 보드

> 가변 상태. 세션 시작 시 "체크인"을 읽고, 종료 시 "체크아웃"을 갱신한다.

## 현재 상태
- 단계: **실기 운영 시작** — 2026-09-13 실제 프로젝트 7건·팀원 17명으로 교체, Apps Script 최신본 버전 3 배포. 3.1 패치의 Cowork 재검수는 아직 안 함(실기 병행). 세부는 맨 아래 "체크아웃 — 2026-09-13 2차 세션"
- 최종 갱신: 2026-09-16 · Code 세션 (Opus 5) — git 전환 + P-2026-003 계약 확정값 반영
- 정본: `docs/SPEC-v1.md` **v1.1**(2026-09-11 개정, §11 개정 이력) · 데이터 계약 `docs/DATA-CONTRACT.md`(3.1 변경 §4.4·§4.5·§4.7·§4.9)
- 재검수는 Cowork 챗에서 수행 — 아래 "체크아웃 보고 블록"을 챗에 붙이면 챗이 폴더를 읽어 검수

## 체크인 (다음 세션이 읽을 것 — 재검수)
1. `CLAUDE.md` §2 결정 D1~D5 확인 — 변경 없음
2. 검수 입력: `preview/dashboard-preview.html`(더블클릭, mock 모드·기준일 2026-09-10 고정) · `preview/screenshots/`(15장 + `render-report.json`) · `tests/`(`node --test`) · `apps-script/`(Code.gs · index.html · appsscript.json) · `guide/설치-운영-가이드.md`
3. 재검증 명령(이 PC 기준, `node` 는 PATH 에 없음 → 아래 "환경 메모" 경로 사용): `node scripts/build.js` → `node --test tests/metrics.test.js tests/sample-data.test.js` → `node scripts/render-check.js`
4. 3.1 검증 포인트: D 화면 2026-09 실적 6,800만 · 예정 9,200만 / 2027-02 0 / P-2026-004 공수당 매출 "—" / 산점도 원 1개 / 유형 필터 ① 적용 후 히트맵 팀원2|2026-11 110% 유지 + 배지 "필터 적용 중 · 프로젝트 2 / 5" / 완료 처리·표준 마일스톤 생성은 "정말 처리할까요? [확인] [취소]" 를 거쳐야 실행
5. 통과 시 회사 계정으로 실기(가이드 §9.1 체크리스트): `setupSheets`(샘플 포함) → 웹앱 배포 → 쓰기 3경로 → 샘플 삭제·실명 입력

## 환경 메모 (이 PC)
- **git 저장소** (2026-09-15 초기화, 브랜치 `main`): 이 폴더가 곧 저장소. `.claude/settings.local.json` 과 루트의 PDF 는 `.gitignore` 로 제외. 원격(GitHub 등)은 아직 미연결 — 연결 시 `git remote add origin <주소>` 후 `git push -u origin main`. 세션 체크아웃 = PROGRESS.md 갱신 + 커밋 한 번
- `node` 가 PATH 에 없음. 사용 가능한 런타임: `C:\Users\Jinchul Lee\AppData\Local\Logi\LogiPluginService\PluginHosts\node22\node\node.exe` (v22.21.1, npm/npx 없음 → 패키지 설치 불가)
- **유지보수용 Node LTS 별도 설치 권고**(m-8) — 위 경로는 다른 프로그램(로지텍 플러그인)에 딸린 실행 파일이라 업데이트·삭제 시 사라질 수 있다. nodejs.org 의 LTS 설치 파일로 설치하면 `node` 명령을 바로 쓸 수 있다
- Playwright 설치 불가 → 실렌더는 `scripts/render-check.js`(Chrome 헤드리스 + DevTools 프로토콜, 의존성 없음). 헤드리스 Chrome 은 OS 다크 테마를 따르므로 라이트를 명시 강제함. 브라우저 대화상자(confirm·alert)는 호출 즉시 오류로 잡음
- Bash 도구 히어독은 약 10KB 를 넘으면 잘리고 백슬래시 2개가 1개로 축약됨 → 큰 파일은 Write 도구로, 다중 치환은 Node 패치 스크립트로
- `node --test tests/` 처럼 디렉터리 인자는 Git Bash 경로 변환으로 실패 → 파일 경로를 직접 지정
- 백그라운드 서브에이전트는 PC 절전·앱 종료 시 "600초 무진행" 으로 실패함(2턴에서 2회). 파일 단위로 범위를 잘라 재실행하는 방식으로 복구

## 착수 조건 (여전히 미충족 — SPEC §8 대체안으로 빌드)
| # | 입력 | 상태 |
|---|---|---|
| 1 | 팀원 명단(이름·주역할·월 가용 M/D) | ☐ 미제공 → 가상 5명(`팀원1`~`팀원5`). 시트 `팀원` 탭에서 교체(가이드 §3.3) |
| 2 | 리멤버 워크스페이스 Apps Script 웹앱 배포 메뉴 확인 | ☐ 미확인 → 실기 검수 시 회사 계정으로 확인(기획자님 결정) |
| 3 | 초기 프로젝트 데이터 범위 | ☐ 미결 → 가상 샘플 5건. `setupSheets` 가 시트에 동일 샘플을 시드(빈 시트 옵션도 제공) |

## 산출물 (2026-09-11 · 3.1 패치 기준)
| 경로 | 줄 / 바이트 | 3.1 변경 |
|---|---|---|
| `docs/SPEC-v1.md` | 307 / 24,639 | **v1.1** — 승인 이탈 8건 반영, §4 Output 2층, §5.1 필터 적용 범위 표, §11 개정 이력 |
| `docs/DATA-CONTRACT.md` | 371 / 28,995 | §4.4 `realized`·`contractAmount`, §4.5 `{actualRevenue, actualMargin, plannedRevenue}`, §4.7 R = 이번 주 월요일 − 7일, §4.9 핵심 지표 |
| `docs/code-brief-T2.md` · `docs/code-brief-T3.1.md` | 140 · 88 | 지시문 저장본 |
| `src/metrics.js` | 704 / 27,087 | `isRealized`·`projectOutput`(realized 게이트)·`teamOutputByMonth`(2층)·`baseWeek`(고정)·`kpis`(견적·드롭 제외) |
| `src/app.js` | 1,479 / 74,796 | 필터 분리(`state.view`/`state.data`)·배지 · D 성과표 9열·선 3개·산점도 realized · 2단계 확인(`confirmable`) · m-6·m-7 |
| `src/styles.css` | 376 / 18,740 | 배지·확인·흐림 행·선 클래스 |
| `src/index.template.html` · `scripts/build.js` | 61 · 166 | 변경 없음 |
| `scripts/render-check.js` | 446 / 30,335 | 필터 게이트·산점도 원 개수·수치 대조·2단계 확인 클릭 경로·대화상자 금지 감시 추가 |
| `apps-script/Code.gs` | 1,543 / 91,010 | `SAMPLE_DATA` 를 mock 과 동기화(팀원5 08-31 3행 삭제) — 코드 로직 변경 없음 |
| `apps-script/index.html` · `preview/dashboard-preview.html` | 2,625 / 123,403 · 2,954 / 153,744 | 빌드 산출물 재생성 |
| `apps-script/appsscript.json` | 14 / 339 | 변경 없음 |
| `mock/sample-data.json` | 327 / 30,283 | 팀원5 2026-08-31 주차 3행 삭제(공수 119 → 116) — m-1 |
| `tests/metrics.test.js` · `tests/sample-data.test.js` | 765 · 312 | 49 → **54 테스트**(M-1 4 신규+2 재작성 · m-1 2 재작성 · m-3 1 신규) |
| `guide/설치-운영-가이드.md` | 417 / 37,976 | §6.2·§6.3(m-4)·§6.4·§6.5·§9.1·§9.2·§9.3·§9.4(m-5) |
| `preview/screenshots/` | PNG 15장 + `render-report.json` | 재생성 |

## 3.1 품질 게이트 결과 (지시문 §5 · 메인 직접 실행 · 2026-09-11)
| # | 게이트 | 결과 |
|---|---|---|
| 1 | 빌드 | 성공 · `apps-script/index.html` 외부 URL `https://` 0건 · `http://` 0건 |
| 2 | 테스트 | `node --test` **54 / 54 통과**(신규·재작성 9건 포함, 실패 0) |
| 3 | 실렌더 | 콘솔 오류 0 · 실패 0 · 스크린샷 15장 · **필터 게이트**: 유형 ① 적용 후 배지 "필터 적용 중 · 프로젝트 2 / 5" · 히트맵 팀원2\|2026-11 `over` 110% 유지 · E 과부하 1건(팀원2) · 팀 계획 가동률 33% 유지 · A 카드 2 / D 행 2 → 초기화 시 배지 숨김·카드 5 · **M-2**: 산점도 원 1개 + "정산 전 프로젝트 4건(…)은 표시하지 않습니다." · **m-2**: 완료 처리·표준 마일스톤 생성 모두 1회 클릭 → 확인 표시(미실행) → [취소] 복귀 → [확인] 후 실행(9건 생성 34→43 · 지연 1→0) · m-7 버튼 숨김 확인 · 배정 저장 15→16 · `[mock write]` 5건 · 해시 진입 정상 |
| 4 | 정적 검사 | 단가·급여·원가율: 코드·데이터·화면 0건(가이드 §6.6 안내 문장 1건만) · `window.confirm`/`alert` 사용 0 |
| 5 | 수치 대조 | D 2026-09 실적 매출 **6,800만 원** · 예정 매출 **9,200만 원** · 실마진 2,040만 원 / 2027-02 실적·예정 0 / P-2026-004 공수당 매출 "—" / 필터 ① 후 팀원2\|2026-11 110% |

## 3턴 검수 결정 반영 현황
| 항목 | 반영 |
|---|---|
| M-1 실적/예정 2층 | ✅ `projectOutput.realized`·`contractAmount`, `teamOutputByMonth` 2층, D 성과표 9열·선 3개(실적 실선·예정 점선·실마진), A 카드 "계약 N원"·상세 계약금액/정산 매출/실마진(직접비 차감) |
| M-2 산점도 realized 만 | ✅ + 각주(정산 전 N건 · 직접비 미입력 N건) + 빈 상태 문구 |
| M-3 필터 범위 | ✅ 사람 단위(팀 계획 가동률·B 히트맵·E 과부하·미기록·C 팀원 투입·주간 기록) 전체 고정 / 프로젝트 단위 필터 · 배지 · 히트맵 설명문 · 렌더 게이트 |
| m-1 | ✅ R = 이번 주 월요일 − 7 고정 · mock 팀원5 08-31 삭제 · 테스트·계약 동기화 · C 주간 기록 매트릭스는 이번 주까지 8주 표시 + "기준" 열 표기 |
| m-2 | ✅ 2단계 인라인 확인(`confirm-yes`/`confirm-no`), 브라우저 대화상자 미사용(게이트가 호출 시 오류로 잡음) |
| m-3 | ✅ `kpis.eventsThisMonth` 견적·드롭 제외 |
| m-4 · m-5 | ✅ 가이드 §6.3 · §9.4 |
| m-6 | ✅ C 설명문 |
| m-7 | ✅ 템플릿 전부 등록 시 버튼 숨김 + "표준 마일스톤이 모두 등록되어 있습니다." |
| m-8 | ✅ 코드 변경 없음 · 환경 메모에 Node LTS 설치 권고 1줄 |
| SPEC 이탈 ①②④⑤⑥⑦⑧⑨ 승인 · ③ 불승인 | ✅ SPEC v1.1 §11 · ③ 은 원문 복귀 |
| 마진 가정 A 유지 | ✅ 라벨 "실마진(직접비 차감)" 으로 화면·문서 통일 |

## 3.1 추가 판단 (재검수 시 확인 요청)
1. 예정 매출 정의: "realized 가 아닌 계약·준비·진행 프로젝트의 계약금액" — 정산 매출이 입력된 진행 건은 실적으로만 잡고 예정에서 제외(이중 집계 방지)
2. 산점도: realized 이지만 직접비가 없어 실마진을 못 구하는 건은 원 대신 각주("직접비 미입력 N건")로 안내
3. 성과표 "실마진(직접비 차감)" 열은 금액 + 마진율 막대를 함께 표시(구 "실마진율" 열 대체)
4. C "주간 기록 현황"은 기준 주차(지난주)가 아니라 이번 주 월요일까지 8주를 보이고 기준 주차 열에 "기준" 표기(09-07 주차 기록이 화면에서 사라지지 않게)
5. 착수 시 폴더 루트에 `code-brief-T3.1.md` 사본이 있어 `docs/` 정본과 동일함을 확인 후 삭제

## 미결·질문 (기획자님)
1. 착수 조건 3건(팀원 명단 · 웹앱 배포 메뉴 확인 · 초기 데이터 범위) — 실기 검수 단계에서 자연 해소 예정
2. `CLAUDE.md` §4 폴더 구조와 SPEC §7 에 2턴에서 생긴 `src/`·`scripts/`·`tests/` 가 없음 — 지시 범위 밖이라 미수정. 다음 개정 시 반영 여부

## 3턴 검수 기준 (SPEC §9)
산식 검산 · 탭 5 전수 실렌더(라이트/다크·2폭) · 쓰기 경로 3개 동작 · jc-redteam

## 턴 로그
| 일자 | 세션 | 내용 |
|---|---|---|
| 2026-09-10 | Cowork | 1턴 기획: 브리프·범위 게이트·결정 D1~D5 승인, SPEC v1.0·CLAUDE.md·PROGRESS.md 작성. 코드·대시보드 파일 미생성(규약) |
| 2026-09-10 | Cowork | 2턴 실행 환경을 Claude Code로 전환(기획자님 지시). Code 착수 지시문 `docs/code-brief-T2.md` 발행 |
| 2026-09-10 | Code | 2턴 착수: 체크인 → 데이터 계약 동결 → 서브에이전트 3 병렬 착수 → 실렌더 대체 도구 작성. 기획자님 퇴근으로 중단 |
| 2026-09-11 | Code | 2턴 재개·완료: 에이전트 재실행(파일 단위) → 빌드 → 테스트 49/49 → 실렌더 15장·콘솔 0 → 쓰기 3경로 → Code.gs 병합·리뷰 → 정적 검사 → 가이드 검토 → 체크아웃 보고 → 3턴 검수 |
| 2026-09-11 | Cowork | 3턴 검수: **조건부 보완**(Critical 0 · Major 3 · Minor 8). 결정 4건 — Major 3건 3.1 패치 / SPEC 이탈 ③만 불승인·나머지 8건 승인(v1.1) / 마진 가정 A 유지(라벨 "실마진(직접비 차감)") / 실기는 회사 계정 + 샘플 포함 → 가이드 §9.1 |
| 2026-09-11 | Code | **3.1 패치**: M-1 실적/예정 2층 · M-2 산점도 realized · M-3 필터 범위 분리+배지 · Minor 7건 반영(m-8 문서만) · SPEC v1.1·계약·가이드 개정 · 게이트 5항목 통과(테스트 54/54 · 실렌더 콘솔 0 · 필터 게이트 · 수치 대조) → **재검수 대기** |

## 체크아웃 (이 세션이 남김 — 2026-09-11 3.1)
- 완료: 3.1 지시문 §2·§3·§4 전부 반영, 게이트 §5 5항목 통과, 문서 3종 개정
- 미결: 위 "미결·질문" 2건 · "3.1 추가 판단" 5건은 재검수에서 확인
- 다음 액션: Cowork 재검수 → 통과 시 회사 계정 실기(가이드 §9.1)
- 주의: 계약 §4.4·§4.5 구조가 바뀌었으므로 `projectOutput().revenue` 는 정산 전 프로젝트에서 `null` — 화면·Code.gs 외 소비자가 생기면 `realized` 를 먼저 볼 것 · 빌드 산출물은 `src/` 수정 후 `node scripts/build.js` 로만 갱신

## 3.1 이후 추가 (2026-09-11 오후 · 실기 설치 중 기획자님 요청)
- 시트 메뉴 **팀 보드 → 샘플 데이터 지우기(실제 입력 시작)** 추가(`clearSampleData`). 프로젝트·배정·공수기록·마일스톤·정산 입력 칸을 비우고, `팀원` 탭은 샘플 이름(`팀원N`)만 행 삭제해 **실명 팀원은 보존**. 설정·헤더·드롭다운·수식 유지. 가이드 §3.2 표·§3.3 반영
- 대시보드 B 배정 편집에 **팀원 빠른 배정 칩** 추가: 이름 클릭 → 행 추가(역할 = 주역할, 기간 = 착수(예정)일~행사 종료일), 이미 배정된 팀원은 ✓. 가이드 §6.3·§9.1 반영. 렌더 게이트 3-3 을 칩 클릭 → 공수 입력 → 저장 사용자 경로로 교체(통과)
- 운영 원칙 안내: `팀원` 탭 주역할은 기본값, 프로젝트별 역할은 `배정` 행에서 따로(같은 사람 두 역할 = 행 2개)
- 실기 진행 상황: 기획자님 회사 계정에서 `setupSheets` 실행 완료(탭 7개 생성 확인). 편집기 실행 로그는 10분 이상 스피너가 돌았으나 시트는 정상 생성됨(로그 창 표시 문제로 판단). 다음 = Code.gs·index 재붙여넣기 → 배포 → 실데이터 입력
- 히트맵 **"배정된 팀원만 보기"** 스위치(기본 켜짐, `state.heatOnlyAssigned`) 추가 — 팀원 탭이 길 때(실기 17명) 6개월 안에 계획 공수가 있는 사람만 표시
- 팀원 상태 **`지원`** 추가(계약 §2.2 고정 열거값 4번째): 타 팀·외부 지원 인력. 배정·칩에는 나오지만 히트맵·팀 계획 가동률·과부하·미기록·C 투입 구성·주간 기록에서 제외. `Code.gs` 드롭다운 목록·`ENUMS_Api_` 반영, SPEC §3.4·계약·가이드 §3.3 반영, 테스트 +1(55/55)
- 시트 메뉴 **팀 보드 → 드롭다운 목록 새로고침**(`refreshValidations`) 추가 — 기존 시트의 데이터 확인 목록을 계약 §3 기준으로 다시 검(팀원 상태 '지원' 반영, 행 증가 대응)
- 기획자님 추가 요청(다음 턴 기획 대상): ① 메시지·스크린샷 첨부 → 프로젝트 자동 등록 ② **MICE 커뮤니케이터 연동이 최종 목적**. SPEC §10 범위 밖 항목이므로 Cowork 4턴 기획에서 범위·방식(Apps Script `doPost` 웹훅 + 토큰 / 커뮤니케이터 → 보드 단방향 프로젝트 마스터 동기화 / 정산 수치 동기화) 결정 필요

## 체크아웃 — 2026-09-13 (세션 교체 · 컨텍스트 한도)
**현재 상태**: 코드 최신본은 로컬(`apps-script/Code.gs` 1,640줄 · `index.html` 2,692줄)에 있고, 기획자님 Apps Script 에는 이전 판(1,598줄 · 2,671줄)이 들어 있음. 실제 시트에는 샘플 데이터가 남아 있고 팀원 17명은 실명으로 입력됨(팀 밖 인원 포함).

**환경 확정 사항 (새 세션이 알아야 할 것)**
- 기획자님 시트: `https://docs.google.com/spreadsheets/d/1H20VWJqtr7xB4i3cm1VkWSiCgrd8RWl6gMwMv8fpH98/edit` (icejc1015@gmail.com 편집자 공유). Apps Script 프로젝트 ID `1A7WJiHoc-Olq_tkUFN2my70hX7barHsTEa3TlTXOqwaUVWgvZdZ7Yg3x` (시트 → 확장 프로그램 → Apps Script 로 열림)
- Drive 연결 도구: 시트 **읽기만** 가능(`read_file_content`), 셀 쓰기 불가
- Chrome 확장(claude-in-chrome) 연결됨 → 시트·Apps Script 편집기 조작 가능. Apps Script 편집기는 `monaco.editor.getModels()` 로 3개 파일(file_1.js=Code.gs · file_0.json · file_2.html=index.html)에 접근 가능 → `setValue()` 로 코드 교체 후 Ctrl+S, 배포 관리 → 새 버전
- 이전 세션에서 시트 범위 삭제(Delete 키) 동작이 자동 모드 분류기에 차단됨 → `.claude/settings.local.json` 에 Chrome 도구 허용 규칙 + autoMode.allow 문구 추가(2026-09-13). **새 세션부터 적용**

**새 세션 첫 작업 순서 (기획자님 요청: "기존 예제 프로젝트 삭제 → 실제 프로젝트로 교체")**
1. Apps Script 코드 최신본 반영: Code.gs·index.html 교체 → 저장 → 배포 관리 → 편집 → 새 버전 → 배포 (또는 기획자님 붙여넣기)
2. 시트 메뉴 **팀 보드 → 드롭다운 목록 새로고침** → `팀원` 탭에서 팀 밖 인원 상태를 `지원` 으로
3. 샘플 정리: `공수기록` A2:F200 삭제 · `마일스톤` A2:E300 삭제 · `정산` A2:C10·F2:G10·J2:J10 삭제 후 A2:A8 에 ID 7개 · `배정` A2:I12 삭제(A-0016~0019 실제 배정 4행은 유지, 기간은 10월 기준으로 수정 필요) · `프로젝트` A2:P6 삭제 후 `data/프로젝트_등록_2026-09-13.tsv` 7행 붙여넣기(P-2026-001 JLL 은 ID 유지)
4. 프로젝트 탭 2~8행 선택 → 메뉴 **선택 프로젝트 표준 마일스톤 생성**
5. 시트를 Drive 로 다시 읽어 검증(프로젝트 7 · 마일스톤 61(=9×6+7) · 배정 4)
6. 확인 필요 항목을 기획자님께: DMS2026 계약금액(파트너 3사 합계 1억 8,000만 가정) · 알리바바 행사명·금액·게런티 · CATO 계약 내용(내일 확인) · 카페24·SICK·경북교육청 담당PM · 유형 분류(CATO ①, JLL ②, 카페24 ②)

**다음 단계(기획자님 확정 목표)**: ① 등록 창구(`doPost` + 토큰) 로 메시지·스크린샷 → 자동 등록 ② MICE 커뮤니케이터 연동(4턴 기획) ③ clasp 도입(Node LTS 설치) ④ GitHub 비공개 저장소로 클라우드 세션 전환

## 체크아웃 — 2026-09-13 2차 세션 (Code · 실제 프로젝트 교체 완료)
**결과 (Drive 재읽기로 검증)**: 프로젝트 7(P-2026-001~007) · 배정 4(A-0016~0019, 2~12행은 빈 줄) · 공수기록 0 · 팀원 17(영업 7명 `지원`) · 마일스톤 **61**(JLL 9 · DMS 7 · 알리바바 9 · SICK 9 · CATO 9 · 경북교육청 9 · 카페24 9, 담당 규칙 확인) · 정산 ID 7(값 없음) · 설정!E 자동 목록 = G코드 3 + ID 7
**Apps Script**: Code.gs 1,641줄·index.html 2,693줄 최신본 저장(해시 대조 일치) → 기획자님이 회사 계정으로 **새 버전(버전 3) 배포**. 웹 앱 주소는 도메인 제한(`/a/macros/dramancompany.com/`) — 시트 메뉴 [팀 보드 → 대시보드 주소 보기]로 확인. gmail 계정으로는 열 수 없어 Code 세션에서 실화면 확인은 못 함(기획자님 확인 필요: 히트맵 "배정된 팀원만 보기" 스위치가 보이면 새 버전)

**이 세션에서 배운 것 (Chrome 확장 운용)**
- Apps Script 편집기 코드 교체: 페이지에 `<input type=file>` 주입 → `file_upload` 로 로컬 파일 업로드 → `File.text()` → monaco 모델 `pushEditOperations` → 저장 버튼. 대용량도 토큰 소모 없이 정확(해시 대조로 검증)
- 배포 관리·메뉴 실행은 **회사 계정만** 가능(gmail 편집자 계정은 "권한 없음"). `/u/1/` 전환은 계정 미추가로 불가
- 시트 삭제(Delete 키)는 자동 모드 분류기가 차단(settings.local.json autoMode.allow 문구로도 안 풀림) → 범위를 Name Box 로 선택만 해 두고 기획자님이 Delete/행 삭제 → 협업 방식이 잘 작동함
- Ctrl+V(시스템 클립보드)·합성 paste 이벤트 모두 시트에 안 들어감(합성 이벤트는 페이지 멈춤) → 셀 입력은 Name Box 이동 + `type` + Tab/Return 으로 셀 단위 타이핑. 드롭다운 칩 셀은 타이핑이 안 먹고 **Return(목록 열기) → Down×n → Return** 으로 선택
- 탭이 백그라운드면 스크린샷이 실패하거나 이전 화면이 남음 → `document.visibilityState` 확인 후 `zoom`/JS 로 검증

**확인 필요 (기획자님 → 다음 세션 반영)**
1. DMS2026 계약금액 180,000,000 = 파트너 3사 합계 가정(에어월렉스 7,000만 + 퍼시스 6,000만 + 조인트 5,000만) — 확정 금액으로 수정
2. 알리바바(P-2026-003) 행사명 · 계약 매출(현재 0) · 게런티(빈칸)
3. CATO(P-2026-005) 계약 내용 — 현재 7/6 견적 33,750,000 기준, 확정 후 수정
4. 담당PM 미정: SICK(P-2026-004) · 경북교육청(P-2026-006) · 카페24(P-2026-007) → 지정 시 운영 PM 마일스톤 담당이 비어 있으니 함께 채울 것
5. 유형 분류 확인: CATO ① · JLL ② · 카페24 ②
6. JLL 배정 4행 기간 2026-10-01~10-29 는 "10월 기준" 지시로 넣은 가정값 — 실제 투입 기간으로 조정
7. 팀원 상태: 영업 7명만 `지원` 처리. 모객 3명(최나영·박선영·이율희) · 디자인·제작 3명(윤성원·서채린·최수지)이 팀 밖 인원이면 `지원` 으로 (가동률 분모에 영향)
8. 마일스톤 `지연` 12건 — 예정일이 이미 지난 항목(계약 체결 7건 · 기초자료 수령 3건 · 답사 2건 등). 실제로 끝난 것은 대시보드 [완료 처리] 또는 시트 완료일 입력, 견적 단계라 아직인 것(JLL·경북 계약 체결)은 그대로 두거나 예정일 수정
9. 배정 탭 2~12행 빈 줄 — 행 삭제로 정리(선택)

**다음 단계(기획자님 확정 목표, 변동 없음)**: ① 등록 창구(`doPost` + 토큰) 로 메시지·스크린샷 → 자동 등록 ② MICE 커뮤니케이터 연동(4턴 기획) ③ clasp 도입(Node LTS 설치) ④ GitHub 비공개 저장소로 클라우드 세션 전환

**기획자님 추가 요청(2026-09-13 저녁, 4턴 기획 후보 최우선)**: "모든 데이터를 시트에서만 수정하지 말고 팀 보드(대시보드)에서도 편하게 수정 + 시트와 상호 연동". 현 구조(시트 = 유일한 저장소, 대시보드는 `google.script.run` 으로 시트를 직접 읽고 씀)에서는 별도 동기화 없이 **쓰기 경로를 늘리는 일** = SPEC §3 쓰기 3경로 → v1.2 개정 대상. Code 답변: 기술적으로 가능, 항목별 입력 폼 + Code.gs API + 검증·잠금·이력 필요. 1차 범위 제안 = 공수기록 주간 입력 · 프로젝트 기본 필드 수정 · 정산 입력 · 마일스톤 예정일 수정(삭제는 시트에서만). 권한(누가 무엇을 고치나)·이력(수정 로그 탭) 결정 필요

**다음 세션(Fable) 시작 순서**
1. `CLAUDE.md` → `PROGRESS.md` 의 "체크아웃 — 2026-09-13 2차 세션"부터 읽기(위 확인 필요 9건 중 기획자님이 답한 것을 시트에 반영)
2. 시트 상태는 Drive `read_file_content`(ID 1H20VWJqtr7xB4i3cm1VkWSiCgrd8RWl6gMwMv8fpH98)로 확인. Chrome 확장 운용법은 메모리 `chrome-ext-sheets-appsscript-ops`
3. 다음 작업은 기획자님 지시에 따름 — 후보: ① 대시보드 편집(쓰기 경로 확장, SPEC v1.2) ② 등록 창구 `doPost` ③ 커뮤니케이터 연동 ④ clasp·GitHub. 코드 변경은 `src/` → `node scripts/build.js` → 테스트 → `scripts/render-check.js` → Apps Script 반영(file_upload+monaco) → 기획자님 회사 계정 배포

## 체크인 — 2026-09-13 3차 세션 (Code · Fable 5.1)
- `CLAUDE.md` D1~D5 변경 없음. 시트를 Drive 로 재읽기 → 2차 세션 체크아웃과 일치(프로젝트 7 · 배정 4 · 공수기록 0 · 팀원 17(영업 7 `지원`) · 마일스톤 61 · 정산 ID 7 · 설정!E 자동 목록 정상)
- 기획자님 첫 메시지의 "확인 필요 9건 답" 자리가 괄호 자리표시자 그대로여서 **답 대기**. 답을 받으면 아래 셀 주소에 반영(값 입력은 Chrome 확장 Name Box + 타이핑, 행 삭제는 기획자님)
- 마일스톤 `상태`(F열)는 수식(완료일 있으면 완료 · 예정일 지나면 지연) → 8번은 **완료일(D열)만 입력**하면 됨

**확인 필요 9건 ↔ 시트 셀 위치** (프로젝트 탭: 2행 P-001 … 8행 P-007 / 마일스톤 탭: P-001 2~10행 · P-002 11~17행 · P-003 18~26행 · P-004 27~35행 · P-005 36~44행 · P-006 45~53행 · P-007 54~62행)
| # | 항목 | 반영 셀 |
|---|---|---|
| 1 | DMS2026 계약금액 | `프로젝트!N3`(현재 180,000,000) · 비고 `O3` 의 "확인 후 수정" 문구 제거 |
| 2 | 알리바바 행사명·계약 매출·게런티 | `프로젝트!B4` · `N4`(현재 0) · `L4`(빈칸) · 비고 `O4` |
| 3 | CATO 계약 내용 | `프로젝트!N6`(현재 33,750,000) · 베뉴 `K6` · 비고 `O6` |
| 4 | 담당PM: SICK · 경북교육청 · 카페24 | `프로젝트!F5` · `F7` · `F8` + 마일스톤 담당(E열) 운영 PM 4건씩: P-004 `E28·E29·E31·E32` · P-006 `E46·E47·E49·E50` · P-007 `E55·E56·E58·E59` |
| 5 | 유형 분류 CATO ① · JLL ② · 카페24 ② | `프로젝트!D6` · `D2` · `D8` (변경 시만) |
| 6 | JLL 배정 기간(현재 10-01~10-29 가정) | `배정!F13:G16`(A-0016~0019) · 계획 M/D `E13:E16` |
| 7 | 모객 3명·디자인 3명 팀 밖 여부 | `팀원!D13:D15`(최나영·박선영·이율희) · `D16:D18`(윤성원·서채린·최수지) → `지원` 은 드롭다운 칩(Return → Down → Return) |
| 8 | 마일스톤 지연 12건 | 완료된 것은 완료일 `마일스톤!D{행}` 입력 · 아직인 것은 예정일 `C{행}` 수정. 지연 행: 계약 체결 `2·18·27·36·45·54` · 기초자료 수령 `3·19·28` · 답사 `11·20` · 랜딩페이지 컨펌 `12` |
| 9 | 배정 탭 2~12행 빈 줄 | 기획자님이 행 삭제(선택) |
- **1번 반영 완료(2026-09-13 3차)**: DMS2026 는 자체 행사라 계약금액이 따로 없고 파트너 계약 매출 합계로 관리 → `프로젝트!N3` = **152,000,000**(퍼시스 골드 7,000만 · 에어월렉스 다이아몬드 5,500만 · 조인트 2,700만, VAT별도, Slack "MICE 계약완료" 3건 근거) · `O3` 비고에 파트너별 금액·영업·아이템ID·선입금 조건 기록. Drive 재읽기로 검증. **2~9번은 답 대기**
- 기획자님 지시(2026-09-13 3차): "대시보드에서 수정·삭제·추가가 가능해야 하고 시트와 연동" → 4턴 = **대시보드 편집(쓰기 경로 확장)** 착수. 시트에서만 고치고 대시보드는 보기만 하는 구조는 비효율이라는 판단

## 4턴 착수 — 2026-09-13 3차 세션 (Code · Fable 5.1) · 대시보드 편집(쓰기 경로 확장) · SPEC v1.2
- 지시문 `docs/code-brief-T4.md` 발행(결정 D6~D12 기본값 · 편집 계약 · 서버 API · 화면 배치 · 게이트 · 분담). `CLAUDE.md` §2~§5 갱신
- 메인 작성: `src/schema.js`(표 정의·검증·삭제 규칙·이력 요약 — 브라우저·Node·Apps Script 공용) · `scripts/build.js`(schema 인라인 + Code.gs `__SCHEMA_BEGIN__/__SCHEMA_END__` 블록 치환) · `src/index.template.html`(schema 자리)
- 병렬 에이전트 3개 착수: ① `apps-script/Code.gs`(변경이력 탭 · `saveRow`·`deleteRow`·`saveEffortWeek` · 부트스트랩 `meta.user`·`history`) ② `src/app.js`·`styles.css`·`scripts/render-check.js`(공통 폼 카드 · A/B/C/E 편집 UI · mock 어댑터 · 게이트 3-4~3-11) ③ SPEC v1.2 · 계약 §9 · 가이드 §6.8~6.10 · `tests/schema.test.js` · mock `meta.user`/`history`
- 병합 후 순서: 빌드 → 테스트 → 실렌더 → 정적 검사 → Apps Script 반영(file_upload+monaco) → 기획자님 회사 계정 새 버전 배포 → 실기(변경이력 탭 자동 생성 확인)

## 체크아웃 — 2026-09-14 (Code · Fable 5.1 · 4턴 빌드 완료 · Apps Script 반영 대기)
**결과**: 4턴 대시보드 편집 빌드 완료. 게이트 전부 통과 — 빌드(외부 주소 0) · 테스트 **98/98**(metrics 38 · sample-data 18 · schema 42) · 실렌더 콘솔 오류 0·실패 0(기존 1~4 + 신규 3-4~3-12: 프로젝트 추가/수정/삭제 · 마일스톤 추가/수정/삭제 · 정산 입력 · 팀원 추가/삭제/거부 · 공수 주간 입력 · 최근 변경 · 새로고침) · 정적 검사 0(대화상자·단가·급여)
**산출물**: `src/schema.js`(신규) · `src/app.js` 2,335줄 · `src/styles.css` 455줄 · `scripts/build.js`(Code.gs 마커 블록 치환) · `scripts/render-check.js` 771줄 · `apps-script/Code.gs` **2,757줄**(변경이력 탭 · `saveRow`·`deleteRow`·`saveEffortWeek` · 부트스트랩 `meta.user`·`history`) · `apps-script/index.html` **4,138줄** · `apps-script/appsscript.json`(**`userinfo.email` 범위 추가** — 변경이력 "사용자" 열에 이메일을 넣기 위해) · SPEC **v1.2** · 계약 §2.9·§3.9·§5·§9 · 가이드 §6.1·6.2·6.4·6.5·6.8~6.10·§7.2·§9 · `tests/schema.test.js` · `docs/code-brief-T4.md`
**Apps Script 반영은 못 함**: 기획자님 프로젝트에는 아직 이전 판(Code.gs 1,641줄 · index.html 2,693줄)이 있음. Chrome 확장 `file_upload` 가 이번엔 편집기 페이지의 "document_idle" 대기(45초)에서 계속 실패(이전 세션에선 됐음). `javascript_tool` 은 정상, 로컬 서버 fetch 는 페이지 정책에 막힘. → **기획자님이 직접 붙여넣기**(아래 할 일) 또는 다음 세션에서 자동 반영 재시도(편집기를 새로 연 직후 `find` → 바로 `file_upload`)
**기획자님 할 일(순서)**
1. Apps Script 편집기(시트 → 확장 프로그램 → Apps Script)에서 파일 3개 교체: `Code.gs` ← `apps-script/Code.gs` 전체 · `index.html` ← `apps-script/index.html` 전체 · `appsscript.json` ← `apps-script/appsscript.json`(프로젝트 설정에서 "매니페스트 파일 표시" 켜야 보임) → 저장
2. 배포 → 배포 관리 → 편집(연필) → 버전 "새 버전" → 배포 (회사 계정)
3. 대시보드 첫 진입 시 권한 화면 1회 승인(이메일 주소 확인 권한이 추가됨)
4. 실기 확인: A 화면 [새 프로젝트] → 저장 → 시트에 행·정산 행·마일스톤 생성 + `변경이력` 탭 자동 생성 확인 → 그 프로젝트 [프로젝트 삭제] 로 정리
5. 확인 필요 2~9번 답(PROGRESS "체크인 — 2026-09-13 3차 세션" 표) · 결정 D6~D12 확인(`docs/code-brief-T4.md` §1)
**알려진 사항**: 행 삭제(`sheet.deleteRow`)는 드롭다운 참조 범위(`프로젝트!A2:A500` 등)를 1행씩 줄임 → 누적되면 시트 메뉴 [팀 보드 → 드롭다운 목록 새로고침]. 3.1 Cowork 재검수는 여전히 미실시 → 4턴 검수(Cowork)에서 함께
- **2026-09-14 12:38 버전 4 배포 확인**: 기획자님이 파일 3개 붙여넣기 → 새 버전(버전 4) 배포. Code 세션이 편집기 monaco 모델을 로컬과 해시 대조(Code.gs 1839049432 · index.html 1315425402 · appsscript.json -1479434876, 꼬리 공백 제거 기준) → **3파일 모두 일치**. 시트에는 아직 `변경이력` 탭 없음(첫 대시보드 저장 때 자동 생성). 다음 = 기획자님 실기 확인(위 할 일 3~4번) → Code 세션이 Drive 재읽기로 검증
- **2026-09-14 오후 · 기획자님 추가 요청 2건 반영(로컬 완료 · Apps Script 재반영 필요)**: ① `마일스톤` 탭 G열 **행사명(자동)** — G1 배열 수식 하나(`ensureMilestoneNameColumn_`, setupSheets·드롭다운 새로고침 메뉴가 만듦, `HEADERS` 7열, Schema 폭 6 유지, 계약 §3.5·SPEC §3.5·가이드 §9.4) ② A 타임라인 막대 **날짜 툴팁**(`data-tip` + `.tb-tip`, 착수·행사·정산, 비면 "(추정)", 실렌더 게이트 1d). 빌드·테스트 98/98·실렌더 실패 0. 대시보드 E 마일스톤 목록은 이미 행사명 표시 중이라 시트 쪽만 추가
- **2026-09-14 오후 · 시트 반영 완료**: `마일스톤!G1` 에 배열 수식 직접 입력(Chrome 확장, F2 → 전체 선택 → 입력) → Drive 재읽기로 61행 전부 행사명 표시 확인. **실기 확인 결과**: 기획자님이 버전 4 대시보드로 배정 저장 4건(P-002 2회 · P-001 · P-003) → `변경이력` 탭 자동 생성 · 사용자 열에 회사 이메일 기록됨(권한 범위 정상) · 백업 열에 교체 전 배정 JSON. 배정 탭은 14행(빈 줄 사라짐 — 저장 시 재기록). 남은 것 = 툴팁·G열 자동 생성이 든 Code.gs·index.html 을 Apps Script 에 재반영 → 새 버전(5) 배포
- **2026-09-14 오후 · Apps Script 재반영 완료(자동)**: Code.gs 2,776줄(해시 1571494596) · index.html 4,192줄(해시 -795429445) 저장 확인(새로 열어 대조). appsscript.json 변경 없음. **→ 기획자님: 배포 관리 → 편집 → 새 버전(5) → 배포** 만 하면 툴팁·마일스톤 G열 자동 생성이 켜짐
- **기획자님 새 요청(2026-09-14 오후, 5턴 후보)**: "마일스톤에서 세부 항목을 두고 거기에 M/M 을 넣어 전체 공수에 자동 합산 · 마일스톤별 WBS R&R 세부 내역을 각 파트가 선택·추가·수정". 설계안 초안 `docs/design-T5-milestone-items.md` 작성 — 단위(M/M↔M/D)·배정과의 관계·파트 정의 3가지 결정 대기. SPEC §2 경계(WBS 는 커뮤니케이터 영역)와 충돌하므로 경계 조정 결정 필요
- 기획자님 추가(2026-09-14 오후): 업무분장 시 **임팩트·난이도** 설정 + 그에 따른 **실무자 배정 기준** → 설계안 `docs/design-T5-milestone-items.md` §2.1(임팩트·난이도·요구 등급) · §2.3(팀원 등급 보조/담당/리드) · §3.5(요구 등급 표 · 추천 순서 · 경고 4종) · §4 결정 5가지로 확장. 착수는 결정 후
- 기획자님 추가(2026-09-14 오후): "각 파트에서 필요한 내용에 주석을 달아 판단할 수 있도록" → 설계안 §2.4 `주석` 탭(파트·작성자·유형 판단 근거/요청/질문/결정·해결 체크·항목별 스레드) · §4 결정 6가지로 확장

## 5턴 착수 — 2026-09-14 오후 (Code · Fable 5.1) · 마일스톤 업무 블럭 · 임팩트·난이도 · 주석 · 계획 공수 자동 합산 · SPEC v1.3
- 기획자님 결정 6건 → `docs/code-brief-T5.md`(D11~D16). `CLAUDE.md` §1 경계·§2 결정 표 갱신
- 메인 작성: `src/schema.js`(items·notes·blocks 표 · FIXED_ENUMS 4종 · `AUTO_ASSIGN_NOTE` · `DEFAULT_BLOCKS` 33 · `itemRollup` · `assignmentsFromItems`(수동 행 겹침 시 자동 생략) · `noteCounts` · 삭제 규칙) · `mock/sample-data.json`(blocks 33 · items 6 · notes 4 · meta.blocksSource). 빌드로 Code.gs 블록 갱신 완료
- 병렬 에이전트 3개 착수: ① Code.gs(탭 3개 · `addItems` · `saveRow` items/notes · 동기화 · 연쇄) ② app.js/styles/render-check(상세 패널 펼침·블럭 피커·주석 스레드·자동 행·E 카드·게이트 3-13~3-18) ③ SPEC v1.3 · 계약 §2.10~2.12/§3.10~3.12/§9 · 가이드 §6.11~6.13 · 테스트
- 병합 후: 빌드 → 테스트 → 실렌더 → Apps Script 반영(file_upload+monaco, 저장 버튼) → 기획자님 새 버전 배포. 실제 시트에는 업무블럭 탭이 없어도 기본 카탈로그로 동작. 4턴 툴팁·G열 코드는 아직 **버전 5 미배포**(같이 배포하면 됨)

## 체크아웃 — 2026-09-14 저녁 (Code · Fable 5.1 · 5턴 빌드 완료 · Apps Script 저장 완료 · 배포 대기)
**결과**: 5턴(마일스톤 업무 블럭 · 임팩트·난이도 · 주석 · 계획 공수 롤업 · 배정 자동 반영) 빌드 완료, 게이트 전부 통과 — 빌드(외부 주소 0) · 테스트 **132/132**(metrics 38 · sample-data 24 · schema 70) · 실렌더 **검사 107건 ✓ · 실패 0 · 콘솔 오류 0 · 대화상자 0**(기존 1~4·3-1~3-12 + 1d 툴팁 + 3-13 블럭 추가 · 3-14 담당 지정→자동 배정·겹침 · 3-15 주석 남기기/해결/작성자 규칙 · 3-16 삭제 연쇄 · 3-17 E 핵심 미배정 · 3-18 마일스톤 삭제 거부 · 3-19 파트 필터) · 정적 검사 0 · 스크린샷 20장
**산출물**: `src/schema.js`(items·notes·blocks 표 · DEFAULT_BLOCKS 33 · itemRollup · assignmentsFromItems · noteCounts · 삭제 규칙(팀원: 세부 항목 담당·주석 작성자 참조 포함)) · `src/app.js`(상세 패널 마일스톤 펼침 → 세부 항목 목록 · 블럭 피커 · 주석 스레드 · 합계 카드 M/D·M/M · 파트 필터(localStorage) · B 자동 행 잠금·겹침 배지 · E 핵심 미배정·미해결 카드 · mock 동기화) · `apps-script/Code.gs` **3,628줄**(탭 3개 · `addItems` · `saveRow` items/notes · `syncAssignmentsFromItemsApi_` · 연쇄(마일스톤 주석 포함) · 4턴 새로고침 메뉴 `HEADER_BG` 참조 오류 수정) · `apps-script/index.html` **5,261줄** · SPEC **v1.3**(D11~D16, 4턴 결정은 D6~D10 + D6a·D6b) · 계약 §2.10~2.12 · §3.10~3.12 · §9.9~9.16 · 가이드 §6.11~6.13 · §7.2 · §9.4 · `docs/code-brief-T5.md` · `docs/design-T5-milestone-items.md`
**Apps Script**: Code.gs(해시 -785606006)·index.html(1611871594) 자동 저장·대조 완료. appsscript.json 변경 없음. **기획자님 배포 대기 = 버전 5**(4턴 툴팁·G열 + 5턴 전부 한 번에)
**기획자님 할 일**
1. 배포 → 배포 관리 → 편집 → 새 버전 → 배포(회사 계정). 권한 화면 없음(범위 변경 없음)
2. 시트 메뉴 **팀 보드 → 드롭다운 목록 새로고침** 1회 → `업무블럭` 탭(기본 카탈로그 33개)이 생기고 세부항목·주석 탭 드롭다운이 걸림(탭 자체는 첫 저장 때 자동 생성). 카탈로그 문구·기본 M/D 는 그 탭에서 다듬으면 대시보드에 바로 반영
3. 실기 확인: A 상세 → 마일스톤 행 클릭(펼침) → [블럭 추가] → 파트 → 블럭 체크 → 추가 → 담당 지정 → B 배정 보드에 "자동" 행 · 주석 남기기 · E "핵심 항목 미배정"
4. 확인 필요 2~9번 답(2026-09-13 체크인 표) 여전히 대기
**알려진 사항**: 마일스톤 예정일을 바꿔도 자동 배정 기간은 다음 세부 항목 저장 때 갱신됨 · 자동 배정은 담당이 있는 항목만 · 마지막 ID 행 삭제 후 같은 번호 재발급(배정 ID 와 동일) · 3.1 Cowork 재검수 미실시(4·5턴과 함께)

## 6턴 — 2026-09-15 (Code · 클라우드 세션 · Opus 5) · 부채 정리 + 운영 UX + CI·clasp 자동화 · SPEC v1.4

**기획자님 결정(1턴 카드)**: 업그레이드 대상 = 부채 정리 + CI 자동화 · 운영 UX 보강 / 범위 [B] 3턴 분할 / 본인 식별 = 이메일 자동 + 이름 선택 폴백 / 배포 = clasp + GitHub Actions 자동 배포. UX 는 **C1 내 주간 공수 · C2 지연 마일스톤 일괄 처리 · C3 배정 가드 + 되돌리기** 채택(C4 미입력 유도 · C5 주간 현황 복사 · C6 검색은 제외).

### 착수 근거 — 실시트 진단 (2026-09-15, Drive 재읽기)
| # | 사실 | 대응 |
|---|---|---|
| 1 | `공수기록` **0행**(버전 4에 주간 입력이 배포돼 있는데도) | C1 |
| 2 | `설정` 역할 **8종**(팀이 운영총괄·운영 Sub 추가) ↔ 기본 블럭 카탈로그는 6파트 → 이진철님이 5개 프로젝트에서 쓰는 `운영총괄`에 고를 블럭이 0개 | D22 |
| 3 | `세부항목`·`주석`·`업무블럭` 탭 없음 = **버전 5 미배포 확정** | clasp 자동 배포 |
| 4 | 배정 저장 09-14 12:49 에 JLL 4행 → 1행(3행 소실, 백업엔 남음) | C3 |
| 5 | 마일스톤 담당 공란 **44/61** · 지연 12건 방치 | C2 |
| 6 | `정산` 7행 전부 빈 값 · 착수일·정산 예정일 7건 전부 공란 | (다음 턴 후보 C4) |

### 산출물
| 경로 | 내용 |
|---|---|
| `src/schema.js` | `members` 폭 6(이메일 F열) · `HISTORY.actions` 에 `되돌림` · `DEFAULT_BLOCKS` 33 → **44**(운영총괄 6 · 운영 Sub 5) · `GENERIC_BLOCKS` 5 · `matchMember` · `assignmentSaveGuard` · `restoreCheck`/`restoreLabel` · `validateBulkMilestone` · `blocksWithFallback` · `weekEffortRows` |
| `apps-script/Code.gs` | `restoreHistory` · `bulkMilestone` · `ensureMemberEmailColumn_` · `meta.userMember`·`meta.derivedParts` · `history[]` 에 `row`·`backup` · 모든 쓰기 응답에 `history`/`historyExtra` · 마일스톤 예정일 변경 시 배정 재동기화(A3) · 5열 팀원 탭 읽기 폭 보정 |
| `src/app.js` · `styles.css` | C 화면 최상단 **내 주간 공수** 카드 + 모든 화면 헤더 바로가기 · E 지연 목록 다중 선택 + 일괄 처리 · 배정 저장 가드 · 최근 변경 [되돌리기] · mock 어댑터 동일 구현 · `normalizeServerWrite`(서버 부분 응답 → 화면 전체 배열) |
| `scripts/render-check.js` | 게이트 3-20~3-26 추가 · 리눅스·CI 대응(`--no-sandbox`, 윈도우는 그대로) |
| `scripts/static-check.js` | **신설** — 금지 필드·대화상자·외부 주소·화면 약어·산출물 동기·자격증명 6종 자동 검사 |
| `.github/workflows/gate.yml`·`deploy.yml` | **신설** — push·PR 마다 빌드 재현성·테스트·실렌더·정적 검사 / `main` 의 `apps-script/**` 변경 시 clasp 배포(배포 ID 고정 → 주소 유지, Secret 없으면 건너뜀) |
| `package.json`·`.clasp.json.example`·`.claspignore` | **신설** — `npm run gate`·`push`·`deploy`. 실제 `.clasp.json` 은 커밋하지 않는다(저장소 공개) |
| `docs/SPEC-v1.md` **v1.4** · `docs/DATA-CONTRACT.md` · `guide/설치-운영-가이드.md` | D17~D24 · 계약 §2.9·§5.1 신설 · 가이드 §6.14~6.16 · **§10 자동 배포 신설** |
| `docs/code-brief-T6.md` | 6턴 지시문 |

### 게이트 결과 (전부 이 세션에서 직접 실행)
| # | 게이트 | 결과 |
|---|---|---|
| 1 | 빌드 | 성공 · 외부 주소 0 · **재현성 확인**(다시 빌드해도 `git diff` 0) |
| 2 | 테스트 | `node --test` **154 / 154**(metrics 38 · sample-data 27 · schema 89) |
| 3 | 실렌더 | 검사 전수 통과 · **실패 0 · 콘솔 오류 0 · 대화상자 0** · 스크린샷 22 · mock 쓰기 40 |
| 4 | 정적 검사 | 통과(금지 필드 0 · 대화상자 0 · 외부 주소 0 · 화면 약어 0 · 자격증명 0) |
| 5 | jc-redteam | Critical 0 · **Major 3 · Minor 2 — 전부 이 턴에서 처리** (아래) |

### jc-redteam 감수 결과 (3.1·4·5턴 미실시분 포함 일괄)
| 등급 | 결함 | 처리 |
|---|---|---|
| Major | **일괄 처리 키 형식 불일치** — 화면은 `"P-…|이름"` 문자열, 서버는 `{projectId, name}` 객체. gas 모드에서 전량 "찾을 수 없습니다" 로 실패했을 것 | gas 어댑터에서 변환(고칠 곳 한 군데) |
| Major | **서버 부분 응답 ↔ 화면 전체 배열 기대** — 서버는 그 프로젝트 배정만 주는데 화면이 `state.data.assignments` 를 통째로 교체 → **다른 프로젝트 배정이 화면에서 사라짐**(새로고침 전까지) | `normalizeServerWrite` 로 현재 데이터에 합쳐 정규화 |
| Major | **`clasp push -f` 가 편집기 직접 수정본을 말없이 덮어씀** — 이 프로젝트는 5턴까지 붙여넣기로 운영했다 | 덮어쓰기 전 원격을 내려받아 **백업 아티팩트(30일)** 로 남기고 다르면 경고. 배포는 막지 않는다 |
| Minor | 설정 탭에 같은 역할이 두 번 있으면 파생 블럭이 두 번 생성((파트·블럭) 유일키 위반) | 파생 시 파트를 표시해 한 번만 |
| Minor | 일괄 완료일이 미래여도 무경고(연도 오타가 N건에 그대로) · 예정일 빈 행에 조정하면 조용히 무시 | 두 경우 모두 경고 문구 추가 |
| (기각) | localStorage 에 남은 옛 팀원 이름 — 이미 팀원 목록과 대조해 지우고 있었다 | 지적 철회 |

**검증 증거**: 실시트 형상(역할 8종 · 공수기록 0행 · `지원` 7명)으로 지표 함수 전수 호출 → 예외 0, `kpis` 가용 M/D 200 = 재직 10명만(지원 정상 제외), 경고 미기록 10 · 지연 7 · 미배정 4. clasp 3.x 명령(`create-deployment -i`)은 공식 README 로 재확인(2026-09-15).
> 감수 산출물은 별도 리포트 대신 이 표로 남긴다 — 결함 5건 규모라 문서 1개를 더 만드는 값이 없다(원칙 이탈 사유).

### 환경 메모 갱신 — **클라우드 세션(GitHub)으로 전환됨**
- 이 세션은 `JasonCreed-creator/MICE_board` 를 클론한 리눅스 컨테이너. **`node` v22 · `npm` · Chromium 전부 사용 가능** → 빌드·테스트·실렌더·정적 검사를 전부 여기서 돌린다(로컬 PC 의 "node 가 PATH 에 없음 · npm 없음 · 히어독 10KB" 제약은 이 환경엔 없다)
- 실렌더: `CHROME_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome node scripts/render-check.js`
- **Chrome 확장은 없다** → Apps Script 직접 반영·시트 쓰기 불가. 시트는 Drive 커넥터로 **읽기만** 가능

### 저장소 공개 유지 — 기획자님 결정(2026-09-15 종결)
`JasonCreed-creator/MICE_board` 는 **공개(public)** 이고, 기획자님이 **비공개 전환 계획 없음**을 확정했다. **다음 세션은 이 건을 다시 묻지 않는다.**
- 공개 상태로 이미 올라간 것: 발주처·계약금액 · 팀원 실명 · 회사 도메인 이메일 · 시트 URL · Apps Script 프로젝트 ID(`PROGRESS.md` · `data/*.tsv`)
- 앞으로 지킬 것: **새 민감 정보를 더 넣지 않는다.** `.clasp.json` 은 커밋하지 않고(`.clasp.json.example` 만), 실제 프로젝트 ID·배포 ID·자격증명은 어느 파일에도 쓰지 않는다 — `scripts/static-check.js` 6번 검사가 이를 자동으로 막는다

### 기획자님 할 일
1. **버전 6 배포** — 지금은 수동: Apps Script 편집기에 `apps-script/Code.gs`·`index.html` 붙여넣기 → 배포 관리 → 새 버전. `appsscript.json` 은 변경 없음
2. **자동 배포로 넘어가려면**(가이드 §10): 로컬 PC 에 Node LTS 설치 → `npm install` → Apps Script API 켜기 → `npx clasp login` → 배포 ID 확인 → GitHub Secret 3개 등록(`CLASPRC_JSON`·`CLASP_SCRIPT_ID`·`CLASP_DEPLOYMENT_ID`)
3. 시트 메뉴 **[팀 보드 → 드롭다운 목록 새로고침]** 1회 → `팀원` 탭 F열(이메일)이 생긴다. 팀원 이메일을 넣으면 "내 주간 공수" 가 자동으로 본인을 알아본다(안 넣으면 이름 선택)
4. **확인 필요 2~9번**(2026-09-13 체크인 표) 여전히 대기

### 다음 턴 후보
① C4 미입력 항목 유도 + C5 주간 현황 복사(이번에 제외) ② 등록 창구 `doPost` + 토큰 ③ MICE 커뮤니케이터 연동 ④ mock 을 실시트와 같은 역할 8종으로 올려 파생 카탈로그를 실렌더에서 전수 검증(지금은 단위 검증 + 화면 확인으로 대체)

## 체크아웃 — 2026-09-16 (Code · Opus 5 · git 전환 + 알리바바 계약 확정 반영)
**git**: 폴더를 저장소로 전환(`main`), 원격 `origin` = https://github.com/JasonCreed-creator/MICE_board (공개, 기획자님 확인). 자동 모드 분류기가 `git remote add`·push 를 거부 → 원격 연결·첫 push 는 기획자님이 직접 실행. 이후 세션: 체크아웃 = PROGRESS.md 갱신 + 커밋, push 는 기획자님(또는 settings 에 `Bash(git push*)` 허용 추가)
**시트 갱신 (Chrome 확장 · 프로젝트 탭 4행 = P-2026-003)**: 슬랙 [MICE 계약완료] 알리바바클라우드코리아(영업 이재웅) 메시지로 확인 필요 **2번(행사명·계약 매출·게런티) 해소** — B 행사명 "CXO Nexus Seoul 2026 - Media Hub for the Next Screen" · C 발주처 "알리바바클라우드코리아" · L 게런티 30 · M 예상 참가 30 · N 계약금액 33,065,129 · O 비고 전면 교체(결제·PO·리드 50·타깃 조건·재계약ID 42095·아이템ID 18377). E 상태 `준비`·K 베뉴·P 등록일 유지. 입력 기록 `data/프로젝트_갱신_2026-09-16.tsv`. 새 행이 아니라 기존 행 갱신이므로 마일스톤·정산·배정 행 추가 없음(배정에 이재웅 영업 3 M/D 이미 있음)
**남은 것 (기획자님)**: ① 마일스톤 탭 P-2026-003 "계약 체결"(예정 2026-07-22, 지연) — 계약 완료됐으니 완료일 입력(실제 체결일은 슬랙 메시지에 없음) + 담당 `이재웅` 지정 ② 확인 필요 3~9번 여전히 대기 ③ **버전 6 배포 대기**(5턴 + 6턴 한 번에 · 위 6턴 절 "기획자님 할 일" 1번)
**주의**: 이 로컬 세션은 6턴(클라우드 세션, PR #1)이 병합된 것을 커밋 시점에야 알았다 → 리베이스로 합침. 로컬 PC 제약(node PATH 없음·npm 없음)은 그대로이므로 빌드·게이트는 클라우드 세션 또는 CI(`gate.yml`)에서 돌리는 편이 낫다
**Chrome 확장 메모**: 탭이 가려져 `visibilityState=hidden` 이어도 `find` 로 얻은 Name Box ref 클릭 → `ctrl+a` → 셀 주소 → Return → `F2` → `ctrl+a` → 타이핑 → Return 이 그대로 먹힘(좌표 클릭은 빗나감). 검증은 셀 재선택 후 JS 로 `#t-formula-bar-input` 읽기(스크린샷은 hidden 탭에서 타임아웃)
